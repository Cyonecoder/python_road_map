1- what is python? 
is a language defined grammar and set of functionalities


2- the canonical reference implementation of python 
the most widely used distrubution of python is open source writen in c in python.org , it include standard library

3- virtual enviroment :
bwfore python run it create a directory in my machine that contain all the code based package and standard library all in this place to have prerequesite functinalities to work 
python is battery included language everything is install in same directory
always  let your python instalation cleasn and create venvs (pipenv || venv)
-- to create venv: <python version path> -v venv <name of venv>
-- to run it: source <path to venv> /bin/activate
-- to stop it: deactivate
-- to delete it: rm -f <name of venv>

to check my python versions : ls -l /usr/bin/python*

4- installing third party library:: third library is a library writen by someone else exemple pandas, numpy ,tz

wee can use pip it has more than 200k package to do it we can type :
-pip install package-name==1.2.3
-pip install package-name<=2.0
-pip install package-name>2.0
we can use a requirements.txt file:
pip install -r requirements.txt

-pip freeze : this command show all packages in my envioroment


5- runing python code: interactive & script mode

